# 프로젝트 회고 (RETROSPECTIVE)

**프로젝트:** YHS — 동아대학교 유학생 행정 안내 RAG 챗봇  
**기간:** 2026년 3월 ~ 6월 (1학기)  
**팀원:** [GitHub Contributors](https://github.com/rhyhyr/yhs/graphs/contributors)

---

## 무엇을 만들었나

동아대학교 유학생이 비자·건강보험·기숙사·장학금 등 행정 절차를 한국어로 물어보면, 관련 법령·학칙 PDF에서 근거를 찾아 답변하는 RAG 챗봇을 만들었다. 그래프 DB(Neo4j)와 벡터 검색을 결합한 하이브리드 검색 엔진 위에 Gemini(→ Ollama 폴백) LLM을 연결했고, FastAPI로 REST API를 제공한다.

---

## 잘 된 것 (Keep)

**하이브리드 검색이 예상보다 효과적이었다.**  
벡터 검색 단독 대비 그래프+벡터 병합 방식이 출처 추적 정확도를 높였다. `[파일명, p.N]` 인용 제공이 가능해져 신뢰도가 올라갔다.

**Fast / Deep 2단계 경로 설계가 효율적이었다.**  
단순 질문은 FAQ 매칭 → 단일 검색으로 1~2초 안에 처리되고, 복잡한 비교·예외 질문만 Deep Path로 전환되는 구조가 실제 latency 로그에서 잘 동작함이 확인됐다.

**CI/CD를 일찍 붙인 것이 팀 협업을 도왔다.**  
Lint(ruff) + 단위 테스트 + 커버리지 게이트를 PR 단계에서 강제하니, 리뷰 시 코드 품질 논쟁이 줄고 로직 논의에 집중할 수 있었다.

**A/B 테스트로 응답 포맷을 데이터로 결정했다.**  
"간결 응답(control)" vs "상세 응답(verbose)" 실험 결과, control이 만족도 4.2 vs 3.5, 성공률 100% vs 80%로 명확히 우위였다. 직관에 의존하지 않고 측정으로 결정한 것이 잘 된 선택이었다.

---

## 아쉬운 것 (Problem)

**초반에 그래프 스키마를 여러 번 갈아엎었다.**  
개체-관계 모델을 처음부터 명확히 정의하지 않아, PDF 인제스트 파이프라인을 2~3회 재작성했다. 사전에 ADR로 데이터 모델을 먼저 결정했다면 시간을 아낄 수 있었다.

**eval 커버리지가 특정 문서에 편중됐다.**  
100개 eval 질문 중 다수가 출입국관리법 시행령에서 나왔고, 기숙사·장학금 등 학교 내부 정보 커버리지가 낮았다. 실제 유학생 FAQ 분포를 먼저 조사하지 않은 것이 원인이었다.

**웹 크롤링 폴백의 품질이 불안정했다.**  
Deep Path 후에도 근거가 부족하면 외부 웹을 크롤링하도록 했는데, 사이트마다 구조가 달라 파싱 실패가 잦았다. 크롤링보다 DB 문서를 더 촘촘히 채우는 것이 나을 뻔했다.

**LLM 비용이 실험 후반에 예상을 초과했다.**  
Gemini API를 eval 루프에서 반복 호출하면서 크레딧이 빠르게 소진됐다. 초반에 Ollama 로컬 모델로 실험하고, API는 최종 검증에만 쓰는 습관을 들였어야 했다.

---

## 다음에 시도할 것 (Try)

- **데이터 모델 ADR 먼저 작성** — 스키마 변경 비용을 줄이기 위해 인제스트 구현 전에 그래프 스키마를 ADR로 확정한다.
- **eval 질문 분포 사전 설계** — 실제 유학생 FAQ 로그나 설문을 기반으로 질문 카테고리 비율을 미리 정한다.
- **Ollama-first 실험 정책** — 신규 기능은 로컬 LLM으로 먼저 smoke test, API는 최종 eval에만 사용한다.
- **문서 DB 자동 갱신** — 학사일정·비자 정책은 매 학기 바뀌므로, PDF 재수집 → 재인제스트 파이프라인을 스케줄 워크플로로 자동화한다.

---

## 숫자로 보는 성과

| 지표 | 값 |
|------|----|
| eval 100문 정답률 (judge 기준) | 측정 완료 (docs/agent_eval_result.md 참조) |
| A/B control 만족도 | 4.2 / 5 |
| Fast Path 평균 응답 시간 | ~1.9s |
| Deep Path 평균 응답 시간 | ~3.1s |
| 단위 테스트 커버리지 게이트 | 80% 이상 (CI 강제) |
| 릴리스 태그 | v1.0.1 |
