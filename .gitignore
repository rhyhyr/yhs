# ── 시크릿 ──────────────────────────────────────────────────────────────
.env
.env.*
!.env.example

# ── Python ─────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.cache/          # ruff·pytest 캐시를 여기로 모음 (pyproject 설정)
.coverage
coverage.xml

# ── Node ───────────────────────────────────────────────────────────────
node_modules/
frontend/dist/

# ── 에디터 / OS ────────────────────────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db

# ── 런타임 산출물 ───────────────────────────────────────────────────────
logs/
data/cache/

# ── 실험 결과 (로컬 전용, 용량이 큼) ─────────────────────────────────────
experiments/results/

# 추출 캐시 — raw.jsonl 은 API 비용이 든 산출물이라 커밋한다.
# validated.jsonl 은 raw 에 규칙만 다시 적용하면 공짜로 다시 만들어진다.
data/extract/validated.jsonl
